import { Roles } from "../../Constants";

const sections = [
  {
    label: "Users",
    key: "Users",
    role: Roles.DEVELOPER_AND_ABOVE,
    url: "/web-template/users",
  },
];

export default sections;
